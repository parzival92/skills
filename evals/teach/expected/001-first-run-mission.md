# Expected Behavior: First Run Requires Mission

A good run should pause and establish the mission before teaching. The response should explain that the workspace needs a concrete learning goal, then ask focused questions about why the user needs TypeScript generics, what they want to build, what they already know, and what constraints they have.

It may draft a `MISSION.md` only after the user answers. It should not create a lesson or learning record yet.
